/**
 * Esta prueba esta comentada para funcionar como ejemplo de una prueba compleja con cookies e interacción de usuario para un componente global
 */
describe('AlertCookie', () => {
  // beforeEach(() => {
  //   cy.visit('/')
  // })
  // it('Shows alert cookie on first navigation', () => {
  //   cy.getCookie('KunticaGalleta').should('equal', 'unaccepted')
  //   cy.get('.alert-cookie')
  //     .as('AlertCookie')
  //     .should('be.visible')
  //     .should('contain', 'Uso de cookies')
  // })
  // it('Sets cookie accepted cookie on accept', () => {
  //   cy.get('.alert-cookie')
  //     .as('AlertCookie')
  //     .find('button')
  //     .should('be.visible')
  //     .click()
  //     .then(() => {
  //       cy.get('@AlertCookie').should('not.exist')
  //       cy.getCookie('KunticaGalleta').should('equal', 'accepted')
  //     })
  // })
  // it('Should accept implicit if i just navigate', () => {
  //   cy.getCookie('KunticaGalleta').should('equal', 'unnacepted')
  //   cy.visit('/blog').then(() => {
  //     cy.getCookie('KunticaGalleta').should('equal', 'implicit')
  //     cy.get('.alert-cookie').should('not.exist')
  //   })
  // })
})
